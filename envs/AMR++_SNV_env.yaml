name: AMR++_SNV_env
channels:
  - bioconda
  - conda-forge
  - anaconda
dependencies:
  # --- core pipeline ---
  - python=3.9
  - bwa
  - samtools

  # --- workflow-specific ---
  - ngless>=1.5          # BAM filtering for metaSNV
  - metasnv>=2.0.1       # For SNV calling

  # --- python libraries ---
  - numpy
  - pandas
